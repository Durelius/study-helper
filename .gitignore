node_modules/
dist/chulastudy
/server
/chulastudy
*.db
*.db-wal
*.db-shm
.DS_Store

# Rendered audiobooks are ~140 MB per course and ship by rsync, not git.
courses/*/audio/

# Rendered source slides, ~20 MB per course, rebuilt by tools/materials.py and
# shipped with the course directory by deploy.sh.
courses/*/materials/
